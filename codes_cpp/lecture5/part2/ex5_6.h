#pragma once

#include <cstddef>
//ex5
void processArray(size_t size,  unsigned int values[]);


//ex6
//1 2
// void processArray(size_t size, const unsigned int* values);

//3 4
// void processArray(size_t size, unsigned int const * values);

//void processArray(size_t size, const unsigned int const * values);